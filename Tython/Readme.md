# Tython
Tython is Python with types

# How to install
1. Make a venv using python3 -m venv venv and source venv/bin/activate
2. install pyinstaller using pip install pyinstaller
3. run pyinstaller --onefile tyc.py
4. test by using ./tyc test.ty
